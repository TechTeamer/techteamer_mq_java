dist: focal
language: java

addons:
  sonarcloud:
    organization: "techteamer"
  apt:
    packages:
      - rabbitmq-server

env:
  - TEST_ENV=travis

before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew sonarqube test
